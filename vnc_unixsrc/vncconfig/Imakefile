
#ifdef SunArchitecture
EXTRA_DEFINES = -D__EXTENSIONS__
#endif

#ifdef LinuxArchitecture
EXTRA_DEFINES = -DUseDevUrandom
#endif

SRCS = vncconfig.c vncExt.c
INCLUDES = -I../include

all:: vncconfig

OBJS = $(SRCS:.c=.o)

DEPLIBS = $(XLIB)
LOCAL_LIBRARIES = $(XLIB)

ComplexProgramTarget(vncconfig)
